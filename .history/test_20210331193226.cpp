#include <iostream>
#include <fstream>
using namespace std;

float* readPointCloud(string fileName){
    ifstream inFile;
    inFile.open(fileName);
    if(inFile.is_open()){

    }
}

int main(){
    string fileName = "milk.txt";
    float* 

    return 0;
}
